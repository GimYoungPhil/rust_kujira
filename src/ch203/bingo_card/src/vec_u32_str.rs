fn main() {
    let a_vec = vec![100, 200, 300];

    for i in a_vec {
        println!("{}", i);
    }

    let s_vec = vec!["dog", "cat", "chicken"];

    for i in s_vec {
        println!("{}", i);
    }
}